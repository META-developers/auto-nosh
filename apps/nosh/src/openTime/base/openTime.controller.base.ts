/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { OpenTimeService } from "../openTime.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { OpenTimeCreateInput } from "./OpenTimeCreateInput";
import { OpenTimeWhereInput } from "./OpenTimeWhereInput";
import { OpenTimeWhereUniqueInput } from "./OpenTimeWhereUniqueInput";
import { OpenTimeFindManyArgs } from "./OpenTimeFindManyArgs";
import { OpenTimeUpdateInput } from "./OpenTimeUpdateInput";
import { OpenTime } from "./OpenTime";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class OpenTimeControllerBase {
  constructor(
    protected readonly service: OpenTimeService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: OpenTime })
  @nestAccessControl.UseRoles({
    resource: "OpenTime",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(@common.Body() data: OpenTimeCreateInput): Promise<OpenTime> {
    return await this.service.create({
      data: {
        ...data,

        timeLapses: {
          connect: data.timeLapses,
        },
      },
      select: {
        hour: true,
        id: true,
        minute: true,

        timeLapses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [OpenTime] })
  @ApiNestedQuery(OpenTimeFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "OpenTime",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<OpenTime[]> {
    const args = plainToClass(OpenTimeFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        hour: true,
        id: true,
        minute: true,

        timeLapses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: OpenTime })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "OpenTime",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: OpenTimeWhereUniqueInput
  ): Promise<OpenTime | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        hour: true,
        id: true,
        minute: true,

        timeLapses: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: OpenTime })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "OpenTime",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: OpenTimeWhereUniqueInput,
    @common.Body() data: OpenTimeUpdateInput
  ): Promise<OpenTime | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          timeLapses: {
            connect: data.timeLapses,
          },
        },
        select: {
          hour: true,
          id: true,
          minute: true,

          timeLapses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: OpenTime })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "OpenTime",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: OpenTimeWhereUniqueInput
  ): Promise<OpenTime | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          hour: true,
          id: true,
          minute: true,

          timeLapses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
