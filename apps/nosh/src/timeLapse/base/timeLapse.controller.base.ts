/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TimeLapseService } from "../timeLapse.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TimeLapseCreateInput } from "./TimeLapseCreateInput";
import { TimeLapseWhereInput } from "./TimeLapseWhereInput";
import { TimeLapseWhereUniqueInput } from "./TimeLapseWhereUniqueInput";
import { TimeLapseFindManyArgs } from "./TimeLapseFindManyArgs";
import { TimeLapseUpdateInput } from "./TimeLapseUpdateInput";
import { TimeLapse } from "./TimeLapse";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TimeLapseControllerBase {
  constructor(
    protected readonly service: TimeLapseService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TimeLapse })
  @nestAccessControl.UseRoles({
    resource: "TimeLapse",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(@common.Body() data: TimeLapseCreateInput): Promise<TimeLapse> {
    return await this.service.create({
      data: {
        ...data,

        close: {
          connect: data.close,
        },

        open: {
          connect: data.open,
        },
      },
      select: {
        close: {
          select: {
            id: true,
          },
        },

        id: true,

        open: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [TimeLapse] })
  @ApiNestedQuery(TimeLapseFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "TimeLapse",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<TimeLapse[]> {
    const args = plainToClass(TimeLapseFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        close: {
          select: {
            id: true,
          },
        },

        id: true,

        open: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TimeLapse })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TimeLapse",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: TimeLapseWhereUniqueInput
  ): Promise<TimeLapse | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        close: {
          select: {
            id: true,
          },
        },

        id: true,

        open: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TimeLapse })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TimeLapse",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: TimeLapseWhereUniqueInput,
    @common.Body() data: TimeLapseUpdateInput
  ): Promise<TimeLapse | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          close: {
            connect: data.close,
          },

          open: {
            connect: data.open,
          },
        },
        select: {
          close: {
            select: {
              id: true,
            },
          },

          id: true,

          open: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TimeLapse })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TimeLapse",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: TimeLapseWhereUniqueInput
  ): Promise<TimeLapse | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          close: {
            select: {
              id: true,
            },
          },

          id: true,

          open: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
